CREATE TABLE users (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   login VARCHAR(30),
   password VARCHAR(120),
   CONSTRAINT pk_users PRIMARY KEY (id)
);

ALTER TABLE users ADD CONSTRAINT uc_f8d2576e807e2b20b506bf6a3 UNIQUE (login);
